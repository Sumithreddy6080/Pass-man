PORT = 3000
MONGODB_URI = mongodb://localhost:27017/mydatabase
JWT_SECRET = mysecretkey
JWT_EXPIRATION = 1d

# The above are example values. You should replace them with your actual configuration.
# Make sure to keep this file secure and do not share it publicly.
# The JWT_SECRET should be a strong secret key for signing JWT tokens.
# The JWT_EXPIRATION can be set to a duration that suits your application needs.